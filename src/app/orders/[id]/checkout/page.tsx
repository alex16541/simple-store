import { PageTitle } from "@/widgets/PageTitle";
import { Container } from "@mui/material";

const OrderChecoutPage = () => {
  return (
    <Container>
      <PageTitle>Order checout page</PageTitle>
    </Container>
  );
};

export default OrderChecoutPage;
